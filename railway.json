{
  "build": {
    "builder": "dockerfile",
    "context": "thredion-engine"
  },
  "deploy": {
    "startCommand": "gunicorn main:app --workers 1 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT --timeout 120",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 10
  }
}
